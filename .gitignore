.DS_Store

# Local design-tooling skills (third-party, ~3.4MB) — not part of the site
.claude/skills/

# Local design concept, not served
milo-brutalist-concept.html

# Production planning notes (client shoots). Only the shoot-plan page is served; the
# printable PDF and the source notes stay local.
produccion/*
!produccion/vogue-loreal/
produccion/vogue-loreal/*
!produccion/vogue-loreal/plan-rodaje-vogue.html

# Local preview scratch file, not site content
test-hero-grid/

# CGI section mock-up, not part of the site yet


# Vector source for the CGI plate; the path itself is inlined in the page
images/site/logo/