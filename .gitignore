.DS_Store

# Local design-tooling skills (third-party, ~3.4MB) — not part of the site
.claude/skills/

# Local design concept, not served
milo-brutalist-concept.html

# Production planning notes (client shoots, not site content)
produccion/

# Local preview scratch file, not site content
test-hero-grid/
